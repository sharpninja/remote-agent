<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RemoteAgent.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RemoteAgent.App"
    Title="RemoteAgent.App"
    FlyoutBehavior="Flyout">

    <Shell.FlyoutContent>
        <ScrollView>
            <VerticalStackLayout Padding="16" Spacing="10">
                <Label Text="Menu" FontSize="20" FontAttributes="Bold" />
                <Label Text="Open Sessions" FontAttributes="Bold" IsVisible="{Binding IsConnected}" />
                <VerticalStackLayout BindableLayout.ItemsSource="{Binding SessionItems}" Spacing="6" IsVisible="{Binding IsConnected}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="*,Auto">
                                <Button Text="{Binding Title}" HorizontalOptions="Fill"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type Shell}}, Path=BindingContext.SelectSessionCommand}"
                                        CommandParameter="{Binding SessionId}" />
                                <Button Grid.Column="1" Text="✕" WidthRequest="40"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type Shell}}, Path=BindingContext.TerminateSessionCommand}"
                                        CommandParameter="{Binding SessionId}" />
                            </Grid>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
                <Button Text="Start Session" Command="{Binding StartSessionCommand}" IsVisible="{Binding IsConnected}" />
                <Button Text="Settings" Command="{Binding NavigateToSettingsCommand}" IsVisible="{Binding IsConnected}" />
                <Button Text="Account Management" Command="{Binding NavigateToAccountCommand}" IsVisible="{Binding IsConnected}" />
            </VerticalStackLayout>
        </ScrollView>
    </Shell.FlyoutContent>

</Shell>
