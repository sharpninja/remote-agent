<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             x:Class="RemoteAgent.App.QrScannerPage"
             Title="Scan QR Code"
             BackgroundColor="Black">
    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
                                        BarcodesDetected="OnBarcodesDetected"
                                        HorizontalOptions="Fill"
                                        VerticalOptions="Fill" />

        <!-- Overlay: aim crosshair hint -->
        <Frame BackgroundColor="Transparent"
               BorderColor="White"
               CornerRadius="12"
               WidthRequest="220"
               HeightRequest="220"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               IsClippedToBounds="False" />

        <VerticalStackLayout VerticalOptions="End" HorizontalOptions="Center" Margin="0,0,0,48" Spacing="12">
            <Label Text="Point at a Remote Agent pairing QR code"
                   TextColor="White"
                   FontSize="14"
                   HorizontalTextAlignment="Center" />
            <Button Text="Cancel"
                    Clicked="OnCancelClicked"
                    BackgroundColor="#CC000000"
                    TextColor="White"
                    CornerRadius="8"
                    Padding="32,12"
                    HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
