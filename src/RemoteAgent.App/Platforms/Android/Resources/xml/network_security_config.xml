<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext for gRPC to host (e.g. 10.0.2.2 for emulator, or LAN IP) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
